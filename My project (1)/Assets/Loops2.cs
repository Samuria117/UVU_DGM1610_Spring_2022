using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // code to be blocked to be executed

        {
            inti = object;
            while (int < 5)
            {
                Console.WriteLine(i);
                int++;
            }
        }
    }

}
